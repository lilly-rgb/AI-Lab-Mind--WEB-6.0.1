/**
 * The version is now hardcoded in index.html to prevent build/fetch errors.
 * This function is kept to avoid breaking the import in index.js, but it no longer does anything.
 */
export async function initVersion() {
  // This function is intentionally left empty.
}